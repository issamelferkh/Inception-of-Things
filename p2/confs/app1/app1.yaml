kind: Deployment
apiVersion: apps/v1
metadata:
  name: app1-app
  labels:
    app: app1-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app1-app
  template:
    metadata:
      labels:
        app: app1-app
    spec:
      containers:
      - name: app1-app
        image: nginx:1.14.2
        # image: paulbouwer/hello-kubernetes:1.10
        # args:
        # - "-text=App 1"
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-volume
        configMap:
          name: app1-html

---
kind: Service
apiVersion: v1
metadata:
  name: app1-service
spec:
  selector:
    app: app1-app
  ports:
  - port: 80

---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: app1-ingress
spec:
  rules:
  - host: app1.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app1-service
            port:
              number: 80

